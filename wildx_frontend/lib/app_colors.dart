import 'package:flutter/material.dart';

// ── Primary greens ──────────────────────────────────────────────────────────
const kGreen = Color(0xFF2E7D32);
const kGreenLight = Color(0xFF4CAF50);
const kGreenSoft = Color(0xFFE8F5E9);
const kGreenDark = Color(0xFF1B5E20);

// ── Earth / nature accents ──────────────────────────────────────────────────
const kEarth = Color(0xFF5D4037);
const kEarthLight = Color(0xFF8D6E63);
const kSand = Color(0xFFF5F0EB);
const kAmber = Color(0xFFF9A825);
const kAmberSoft = Color(0xFFFFF8E1);

// ── Neutral text hierarchy ──────────────────────────────────────────────────
const kTextPrimary = Color(0xFF1C1C1E);
const kTextSecondary = Color(0xFF636366);
const kTextHint = Color(0xFF8E8E93);
const kDividerColor = Color(0xFFE5E5EA);

// ── Backgrounds ─────────────────────────────────────────────────────────────
const kBackground = Color(0xFFF8FAF5);
const kCardBackground = Colors.white;
const kShadowColor = Color(0x14000000); // 8% black

// ── Spacing scale ───────────────────────────────────────────────────────────
const kSpaceXS = 4.0;
const kSpaceSM = 8.0;
const kSpaceMD = 12.0;
const kSpaceLG = 16.0;
const kSpaceXL = 20.0;
const kSpaceXXL = 24.0;
const kSpaceHuge = 32.0;

// ── Border radii ────────────────────────────────────────────────────────────
const kRadiusSM = 8.0;
const kRadiusMD = 12.0;
const kRadiusLG = 16.0;
const kRadiusXL = 20.0;
const kRadiusFull = 100.0;
