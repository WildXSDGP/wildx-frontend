class SafetyTip {
  final String tip;

  const SafetyTip({required this.tip});
}

final List<SafetyTip> wildlifeSafetyTips = [
  const SafetyTip(tip: 'Stay calm and don\'t panic'),
  const SafetyTip(tip: 'Do not run from wild animals'),
  const SafetyTip(tip: 'Make yourself appear larger if threatened'),
  const SafetyTip(tip: 'Back away slowly while facing the animal'),
  const SafetyTip(tip: 'Use the emergency whistle to alert others'),
  const SafetyTip(tip: 'Wait for park rangers assistance'),
];
